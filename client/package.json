{
  "name": "@sp/client",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vue-tsc && vite build",
    "clean": "rimraf node_modules dist",
    "dev": "vite",
    "lint": "eslint --cache --max-warnings 0 \"{src,mock}/**/*.{vue,ts,tsx}\" --fix && prettier --write . && stylelint \"**/*.{vue,css,less,scss}\" --fix --cache --cache-location node_modules/.cache/stylelint/",
    "preview": "vite preview"
  },
  "dependencies": {
    "@sp/components": "workspace:^"
  }
}
