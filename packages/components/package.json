{
  "name": "@sp/components",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "clean": "rimraf --force node_modules dist",
    "dev": "vite",
    "lint": "eslint --max-warnings 0 \"src/**/*.{vue,ts,tsx}\" --fix && prettier --write ."
  }
}
