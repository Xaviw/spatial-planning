{
  "name": "@sp/components",
  "version": "1.0.0",
  "description": "vue组件库",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "clean": "rimraf node_modules dist",
    "lint": "eslint --max-warnings 0 \"src/**/*.{vue,ts,tsx}\" --fix && prettier --write ."
  },
  "dependencies": {
    "echarts": "^5.4.3",
    "vue-echarts": "^6.6.1"
  }
}
