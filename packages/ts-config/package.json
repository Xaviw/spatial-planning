{
  "name": "@sp/ts-config",
  "version": "1.0.0",
  "private": true,
  "files": [
    "base.json",
    "vue.json"
  ],
  "scripts": {
    "clean": "rimraf --force node_modules",
    "lint": "eslint --max-warnings 0 --fix && prettier --write ."
  }
}
