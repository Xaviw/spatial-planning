{
  "name": "@sp/ts-config",
  "version": "1.0.0",
  "private": true,
  "files": [
    "base.json",
    "node.json",
    "vue-app.json"
  ],
  "scripts": {
    "clean": "rimraf node_modules",
    "lint": "eslint --cache --max-warnings 0 --fix && prettier --write ."
  }
}
