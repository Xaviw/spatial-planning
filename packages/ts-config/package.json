{
  "name": "@sp/ts-config",
  "version": "1.0.0",
  "private": true,
  "description": "ts-config配置库",
  "files": [
    "base.json",
    "vue.json"
  ],
  "scripts": {
    "clean": "rimraf node_modules",
    "lint": "eslint --max-warnings 0 --fix && prettier --write ."
  }
}
